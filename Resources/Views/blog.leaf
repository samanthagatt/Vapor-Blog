#extend("index"):
#export("body"):

<section id="blog" class="wrapper">

  <h2>Blog</h2>

  #for(post in posts):
  <article>
    <a href="/#(post.slug)">
      <img src="#(post.image)">
      <h3>#(post.title)</h3>
      <p>#(post.excerpt)</p>
      <p class="category">
        #if(post.category != nil):
          #(post.category)
        #else:
          Uncategorized
        #endif
      </p>
    </a>
  </article>
  #endfor

</section>

#endexport
#endextend